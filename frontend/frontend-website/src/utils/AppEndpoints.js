export const url = "http://192.168.1.3";
const port = "53026";

export const search = `${url}:${port}/api`;
export const updateRating = `${url}:${port}/api/Rating`;
export const product = `${url}:${port}/api/Product`;
export const recommended = `${url}:${port}/api/Recommendation`;

// to jest jakiś żart, że ja to wgl musze obsługiwać
export const category = `${url}:${port}/api/Category`;
export const rating = `${url}:${port}/api/Rating`;